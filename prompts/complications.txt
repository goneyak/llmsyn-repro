Assign 0-6 ICD-9 comorbidities per patient based on their demographics and main diagnosis.
Use only publicly available clinical knowledge (e.g., Mayo Clinic). Do NOT use real patient data.

Allowed ICD-9 codes (top-100 list):
{icd_top}

Patient info CSV (includes demographics and MAIN_DIAGNOSIS):
{diag_csv}

Output format: return ONLY a valid CSV with the same 100 rows.
Columns (in this exact order): {comorb_cols}
Add the COMORBIDITIES column as a pipe-separated list of ICD-9 codes (e.g. 4019|25000). Use an empty string if none.
Do not include commentary or markdown fences.
