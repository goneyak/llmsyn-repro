You are a professional medical coder assigning a primary ICD-9 diagnosis to each synthetic patient based on their demographics.
Use only publicly available clinical knowledge (e.g., Mayo Clinic guidelines). Do NOT use real patient data.

Use ONLY ICD-9 codes from this top-100 list:
{icd_top}

Patient demographics CSV:
{demo_csv}

Output format: return ONLY a valid CSV with the same 100 rows.
Columns (in this exact order): {diag_cols}
Add the MAIN_DIAGNOSIS column (ICD-9 code from the list above). Do not include commentary or markdown fences.
