Assign up to 6 ICD-9 procedure codes per patient based on their demographics, main diagnosis, and comorbidities.
Use only publicly available clinical knowledge (e.g., Mayo Clinic). Do NOT use real patient data.

Allowed ICD-9 procedure codes (reference list):
{proc_master}

Patient info CSV (includes demographics, MAIN_DIAGNOSIS, and COMORBIDITIES):
{comorb_csv}

Output format: return ONLY a valid CSV with the same 100 rows.
Columns (in this exact order): {proc_cols}
Add the PROCEDURES column as a pipe-separated list of ICD-9 procedure codes (e.g. 9904|3323). Use an empty string if none.
Avoid clinically implausible combinations (e.g., appendectomy for a patient with pneumonia).
Do not include commentary or markdown fences.
